// this will get the value of the selected item in a radio group named available
$('input[name="available"]:checked').val(); 


// iterate each element
$('div').each(function() { 
	alert($(this).html());
});

/** selecting by attribute value
 * Select all a tag thas has a title attribute or a value with
 */
$('a[title]')
$('a[title="Progmramming Info"]')

